apiVersion: v1
kind: Service
metadata:
  name: {{ include "reshapr-ctrl.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "reshapr-ctrl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ctrl.service.type }}
  ports:
    - port: {{ .Values.ctrl.service.port }}
      targetPort: http2
      protocol: TCP
      name: http2
  selector:
    {{- include "reshapr-ctrl.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "reshapr-ctrl.fullname" . }}-hz
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "reshapr-ctrl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ctrl.service.type }}
  ports:
    - port: 5556
      targetPort: http2-hazelcast
      protocol: TCP
      name: http2-hazelcast
  selector:
    {{- include "reshapr-ctrl.selectorLabels" . | nindent 4 }}
