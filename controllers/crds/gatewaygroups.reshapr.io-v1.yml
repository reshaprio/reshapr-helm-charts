# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "gatewaygroups.reshapr.io"
spec:
  group: "reshapr.io"
  names:
    kind: "GatewayGroup"
    plural: "gatewaygroups"
    shortNames:
    - "gatewaygroup"
    singular: "gatewaygroup"
  scope: "Namespaced"
  versions:
  - name: "v1alpha1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              keepOnDelete:
                description: "Flag to keep GatewayGroup when deleting this resource.\
                  \ Default is false"
                type: "boolean"
              labels:
                additionalProperties:
                  type: "string"
                description: "The labels attached to this GatewayGroup (key=value\
                  \ pairs)"
                type: "object"
              name:
                description: "The name of the GatewayGroup if you need to override\
                  \ the one from metadata.name"
                type: "string"
            type: "object"
          status:
            properties:
              gatewayGroupId:
                description: "Identifier of the corresponding GatewayGroup in the\
                  \ reShapr control plane"
                type: "string"
              message:
                description: "Detailed message about the current status"
                type: "string"
              observedGeneration:
                description: "Reconciled generation"
                type: "integer"
              status:
                description: "Global status of the reconciliation"
                enum:
                - "ERROR"
                - "IN_PROGRESS"
                - "PREEXISTING"
                - "READY"
                - "UNKNOWN"
                type: "string"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
