# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "services.reshapr.io"
spec:
  group: "reshapr.io"
  names:
    kind: "Service"
    plural: "services"
    shortNames:
      - "service"
    singular: "service"
  scope: "Namespaced"
  versions:
    - name: "v1alpha1"
      schema:
        openAPIV3Schema:
          properties:
            spec:
              properties:
                excludedOperations:
                  description: "The list of excluded operations"
                  items:
                    type: "string"
                  type: "array"
                includedOperations:
                  description: "The list of included operations"
                  items:
                    type: "string"
                  type: "array"
                keepOnDelete:
                  description: "Flag to keep Service when deleting this resource. Default\
                  \ is false"
                  type: "boolean"
                name:
                  description: "The name of the service if you need to override the\
                  \ one from the artifact"
                  type: "string"
                secretRef:
                  description: "Reference to a Secret for accessing the artifact url"
                  type: "string"
                url:
                  description: "The URL to access this service remote artifact definition"
                  type: "string"
                version:
                  description: "The version of the service if you need to override the\
                  \ one from the artifact"
                  type: "string"
              type: "object"
            status:
              properties:
                message:
                  description: "Detailed message about the current status"
                  type: "string"
                observedGeneration:
                  description: "Reconciled generation"
                  type: "integer"
                serviceId:
                  description: "Identifier of the corresponding Service in the reShapr\
                  \ control plane"
                  type: "string"
                status:
                  description: "Global status of the reconciliation"
                  enum:
                    - "ERROR"
                    - "IN_PROGRESS"
                    - "PREEXISTING"
                    - "READY"
                    - "UNKNOWN"
                  type: "string"
              type: "object"
          type: "object"
      served: true
      storage: true
      subresources:
        status: {}
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "services.reshapr.io"
spec:
  group: "reshapr.io"
  names:
    kind: "Service"
    plural: "services"
    shortNames:
      - "service"
    singular: "service"
  scope: "Namespaced"
  versions:
    - name: "v1alpha1"
      schema:
        openAPIV3Schema:
          properties:
            spec:
              properties:
                excludedOperations:
                  description: "The list of excluded operations"
                  items:
                    type: "string"
                  type: "array"
                includedOperations:
                  description: "The list of included operations"
                  items:
                    type: "string"
                  type: "array"
                keepOnDelete:
                  description: "Flag to keep Service when deleting this resource. Default\
                  \ is false"
                  type: "boolean"
                name:
                  description: "The name of the service if you need to override the\
                  \ one from the artifact"
                  type: "string"
                secretRef:
                  description: "Reference to a Secret for accessing the artifact url"
                  type: "string"
                url:
                  description: "The URL to access this service remote artifact definition"
                  type: "string"
                version:
                  description: "The version of the service if you need to override the\
                  \ one from the artifact"
                  type: "string"
              type: "object"
            status:
              properties:
                message:
                  description: "Detailed message about the current status"
                  type: "string"
                observedGeneration:
                  description: "Reconciled generation"
                  type: "integer"
                serviceId:
                  description: "Identifier of the corresponding Service in the reShapr\
                  \ control plane"
                  type: "string"
                status:
                  description: "Global status of the reconciliation"
                  enum:
                    - "ERROR"
                    - "IN_PROGRESS"
                    - "PREEXISTING"
                    - "READY"
                    - "UNKNOWN"
                  type: "string"
              type: "object"
          type: "object"
      served: true
      storage: true
      subresources:
        status: {}
