# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "customtools.reshapr.io"
spec:
  group: "reshapr.io"
  names:
    kind: "CustomTools"
    plural: "customtools"
    shortNames:
    - "customtools"
    singular: "customtools"
  scope: "Namespaced"
  versions:
  - name: "v1alpha1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              customTools:
                additionalProperties:
                  properties:
                    arguments:
                      additionalProperties:
                        type: "object"
                      description: "The target tool arguments result. Actually a template\
                        \ reusing ${input} notation."
                      type: "object"
                    description:
                      description: "Human readable long description of this custom\
                        \ tool."
                      type: "string"
                    input:
                      description: "A JSON Schema object defining the expected parameters\
                        \ for the tool."
                      properties:
                        properties:
                          additionalProperties:
                            additionalProperties:
                              type: "object"
                            type: "object"
                          description: "The properties defining the expected parameters\
                            \ for the tool."
                          type: "object"
                        required:
                          description: "The list of required parameter names."
                          items:
                            type: "string"
                          type: "array"
                        type:
                          description: "The type of the input schema. Must be 'object'."
                          type: "string"
                      type: "object"
                    script:
                      description: "A custom logic executed representing multi-tool\
                        \ orchestration"
                      type: "string"
                    tool:
                      description: "The name of the target tool, e.g., 'user', 'createIssue',\
                        \ etc."
                      type: "string"
                    tools:
                      description: "The exhaustive list of tools the script is allowed\
                        \ to call. Required when 'script' is set."
                      items:
                        properties:
                          service:
                            description: "The target service for cross-service calls,\
                              \ expressed as the readable couple '<service_name:service_version>'.\
                              \ Omit for a tool of the same service."
                            type: "string"
                          tool:
                            description: "The name of the target tool to call."
                            type: "string"
                        type: "object"
                      type: "array"
                  type: "object"
                description: "The custom tools are defined with name as key."
                type: "object"
              service:
                description: "Holds reference information about this Service custom\
                  \ tools relate."
                properties:
                  name:
                    description: "Human readable name of the target Service for custom\
                      \ tools."
                    type: "string"
                  version:
                    description: "Human readable version of the target Service for\
                      \ custom tools."
                    type: "string"
                type: "object"
            type: "object"
          status:
            properties:
              artifactId:
                type: "string"
              message:
                type: "string"
              serviceId:
                type: "string"
              state:
                enum:
                - "ERROR"
                - "IN_PROGRESS"
                - "PREEXISTING"
                - "READY"
                - "UNKNOWN"
                type: "string"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
