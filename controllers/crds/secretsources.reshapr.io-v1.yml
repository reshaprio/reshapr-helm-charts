# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "secretsources.reshapr.io"
spec:
  group: "reshapr.io"
  names:
    kind: "SecretSource"
    plural: "secretsources"
    shortNames:
    - "secretsource"
    singular: "secretsource"
  scope: "Namespaced"
  versions:
  - name: "v1alpha1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              keepOnDelete:
                description: "Flag to keep control plane Secrets when deleting this\
                  \ resource. Default is false"
                type: "boolean"
              secrets:
                description: "The list of secrets to synchronize with the reShapr\
                  \ control plane"
                items:
                  properties:
                    certPem:
                      description: "A certificate or certificate chain in PEM format\
                        \ for a Secret holding TLS authentication information"
                      type: "string"
                    description:
                      description: "The description of the Secret to create in the\
                        \ reShapr target instance"
                      type: "string"
                    name:
                      description: "The name of the Secret to create in the reShapr\
                        \ target instance"
                      type: "string"
                    oauth2ClientConfiguration:
                      description: "OAuth2 client configuration carried by this Secret"
                      properties:
                        authorizationEndpoint:
                          description: "Authorization endpoint of the OAuth2 authorization\
                            \ server"
                          type: "string"
                        clientId:
                          description: "OAuth2 Client ID"
                          type: "string"
                        clientSecret:
                          description: "OAuth2 Client Secret"
                          type: "string"
                        tokenEndpoint:
                          description: "Token exchange endpoint of the OAuth2 authorization\
                            \ server"
                          type: "string"
                      type: "object"
                    password:
                      description: "A password for a Secret holding basic authentication\
                        \ information. Must be provided with username"
                      type: "string"
                    token:
                      description: "A token for a Secret holding token-based authentication\
                        \ information"
                      type: "string"
                    tokenHeader:
                      description: "Header used to transport the token of a Secret\
                        \ holding token-based authentication information"
                      type: "string"
                    type:
                      description: "The type of the Secret — one of ARTIFACT or ENDPOINT.\
                        \ Defaults to ENDPOINT"
                      type: "string"
                    useElicitation:
                      description: "Whether the Secret should be resolved through\
                        \ elicitation at runtime instead of being stored in the control\
                        \ plane"
                      type: "boolean"
                    username:
                      description: "A username for a Secret holding basic authentication\
                        \ information"
                      type: "string"
                    valuesFrom:
                      description: "Optional reference to a Kubernetes Secret from\
                        \ which the values must be loaded at reconcile time"
                      properties:
                        certPemKey:
                          description: "The Secret key containing a certificate or\
                            \ certificate chain in PEM format for a Secret holding\
                            \ TLS authentication information"
                          type: "string"
                        oauth2ClientSecretKey:
                          description: "The Secret key containing the OAuth2 client\
                            \ secret to inject into the OAuth2 client configuration"
                          type: "string"
                        passwordKey:
                          description: "The Secret key containing a password for a\
                            \ Secret holding basic authentication information"
                          type: "string"
                        secretRef:
                          description: "The name of the source Kubernetes Secret to\
                            \ synchronize into reShapr"
                          type: "string"
                        tokenHeaderKey:
                          description: "The Secret key containing the header used\
                            \ to transport a token of a Secret holding token-based\
                            \ authentication information"
                          type: "string"
                        tokenKey:
                          description: "The Secret key containing a token for a Secret\
                            \ holding token-based authentication information"
                          type: "string"
                        usernameKey:
                          description: "The Secret key containing a username for a\
                            \ Secret holding basic authentication information"
                          type: "string"
                      type: "object"
                  type: "object"
                type: "array"
            type: "object"
          status:
            properties:
              conditions:
                description: "Per-secret conditions — one entry per secret name with\
                  \ its control plane id in message"
                items:
                  properties:
                    lastTransitionTime:
                      description: "ISO-8601 timestamp of the last transition of this\
                        \ condition"
                      type: "string"
                    message:
                      description: "Human-readable message about the condition (often\
                        \ the control plane resource id)"
                      type: "string"
                    status:
                      description: "Status of the condition"
                      enum:
                      - "ERROR"
                      - "IN_PROGRESS"
                      - "PREEXISTING"
                      - "READY"
                      - "UNKNOWN"
                      type: "string"
                    type:
                      description: "Type of the condition — typically the sub-resource\
                        \ name it refers to"
                      type: "string"
                  type: "object"
                type: "array"
              message:
                description: "Detailed message about the current status"
                type: "string"
              observedGeneration:
                description: "Reconciled generation"
                type: "integer"
              status:
                description: "Global status of the reconciliation"
                enum:
                - "ERROR"
                - "IN_PROGRESS"
                - "PREEXISTING"
                - "READY"
                - "UNKNOWN"
                type: "string"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
